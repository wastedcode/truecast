# Internal working docs — discovery, interviews, specs, scratch.
# Not part of the shipped/published repo.
internal/

# Throwaway spikes
spike/

# Editor swap / scratch files
*.swp
*.swo
*~
.DS_Store

# Build artifacts / deps
node_modules/
dist/
coverage/
*.tsbuildinfo

# npm artifact — this repo uses pnpm (pnpm-lock.yaml); `npm link` from the install docs writes this
package-lock.json

# Local Claude Code runtime cruft (scheduler lock, transient agent state) — not the repo's
.claude/
